const i18nConfig = {
  defaultLocale: "en",
  locales: ["en", "no"],
  localePath: "./public/locales", // Add this line if your i18n loader supports it
  // prefixDefault: true, // Uncomment if you want /en as default prefix in URLs
  // other i18n configurations
};

export default i18nConfig;
